ALTER TABLE `srampos_category_mapping` ADD `variant_id` INT NOT NULL AFTER `product_id`;

ALTER TABLE `srampos_pro_request_items` ADD `variant_id` INT NOT NULL AFTER `product_id`;

ALTER TABLE `srampos_pro_quote_items` ADD `variant_id` INT NOT NULL AFTER `product_id`; 

ALTER TABLE `srampos_pro_purchase_order_items` ADD `variant_id` INT NOT NULL AFTER `product_id`; 

ALTER TABLE `srampos_pro_purchase_invoice_items` ADD `variant_id` INT NOT NULL AFTER `product_id`; 

ALTER TABLE `srampos_pro_purchase_return_items` ADD `variant_id` INT NOT NULL AFTER `product_id`; 

ALTER TABLE `srampos_pro_stock_master` ADD `variant_id` INT NOT NULL AFTER `product_id`; 

ALTER TABLE `srampos_pro_stock_ledger` ADD `variant_id` INT NOT NULL AFTER `product_id`; 

