ALTER TABLE `srampos_shifts_settlement` ADD `giftvoucher_open` DECIMAL(10,2) NOT NULL AFTER `cash_difference`, ADD `giftvoucher_actual` DECIMAL(10,2) NOT NULL AFTER `giftvoucher_open`, ADD `giftvoucher_received` DECIMAL(10,2) NOT NULL AFTER `giftvoucher_actual`, ADD `giftvoucher_difference` DECIMAL(10,2) NOT NULL AFTER `giftvoucher_received`, ADD `wallet_open` DECIMAL(10,2) NOT NULL AFTER `giftvoucher_difference`, ADD `wallet_actual` DECIMAL(10,2) NOT NULL AFTER `wallet_open`, ADD `wallet_received` DECIMAL(10,2) NOT NULL AFTER `wallet_actual`, ADD `wallet_difference` DECIMAL(10,2) NOT NULL AFTER `wallet_received`;

ALTER TABLE `srampos_permissions` ADD `shiftmaster-index` TINYINT NOT NULL AFTER `bbq_daywise_discount_edit_permission`, ADD `shiftmaster-add` TINYINT NOT NULL AFTER `shiftmaster-index`, ADD `shiftmaster-edit` TINYINT NOT NULL AFTER `shiftmaster-add`, ADD `shiftmaster-delete` TINYINT NOT NULL AFTER `shiftmaster-edit`, ADD `pos-shift_create` TINYINT NOT NULL AFTER `shiftmaster-delete`, ADD `pos-shift_settlement` TINYINT NOT NULL AFTER `pos-shift_create`, ADD `pos-shift_view` TINYINT NOT NULL AFTER `pos-shift_settlement`, ADD `pos-shift_reprint` TINYINT NOT NULL AFTER `pos-shift_view`;

